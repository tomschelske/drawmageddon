{
  "name": "drawmageddon-frontend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --outfile=../src/main/resources/static/js/app.js",
    "watch": "esbuild src/main.ts --bundle --sourcemap --outfile=../src/main/resources/static/js/app.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stomp/stompjs": "^7.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
